export default function taskBlock(trueOrFalse) {
  const task = false;
  const task2 = true;

  if (trueOrFalse) {
    // Bu blok boş qalır, çünki dəyişənlər dəyişdirilməməlidir
  }

  return [task, task2];
}
